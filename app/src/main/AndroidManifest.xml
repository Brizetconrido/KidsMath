<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.kidsmath">

    <!-- PERMISOS -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>

    <!-- Permiso opcional para guardar fotos -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28"
        tools:ignore="ScopedStorage" />

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28"/>


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.KidsMath">

        <!-- Activity Principal -->
        <activity
            android:name=".MainMenuActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Activities del proyecto -->
        <activity android:name=".GameSelectionActivity"/>
        <activity android:name=".ProfileActivity"/>
        <activity android:name=".ScoreActivity"/>
        <activity android:name=".RewardsActivity"/>
        <activity android:name=".RecorderActivity"/>

        <!-- Juegos -->
        <activity android:name=".SumGameActivity"/>
        <activity android:name=".SubGameActivity"/>
        <activity android:name=".MultiGameActivity"/>
        <activity android:name=".DivGameActivity"/>
        <activity android:name=".CountGameActivity"/>
        <activity android:name=".MissingNumberActivity"/>
        <activity android:name=".ColorGameActivity"/>
        <activity android:name=".ShapeGameActivity"/>

    </application>

</manifest>


